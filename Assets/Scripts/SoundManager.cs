using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class SoundManager : MonoBehaviour {
    TextBoxManager textClass;

    void Update()
    {
        if (textClass.currentLine > textClass.endAtLine)
        {

        }
    }
}
